import type { Route } from ".react-router/types/app/+types/root"

export async function loader({context, request}: Route.LoaderArgs) {
  
}

const SearchCheatSheet = () => {
  return (
    <div>SearchCheatSheet</div>
  )
}
export default SearchCheatSheet